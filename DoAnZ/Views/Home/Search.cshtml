
@model IEnumerable<DoAnZ.Models.Product>
@{
    ViewData["Title"] = "Home Page";
    int numOfQuantity = 1;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <!-- CSS Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom CSS */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
        }

        .product-card {
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
        }

            .product-card:hover {
                transform: translateY(-5px);
            }

        .product-image {
            object-fit: cover;
            height: 200px;
            width: 100%;
        }

        .product-title {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .product-price {
            font-size: 1rem;
        }

        .product-add-to-cart {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Search Results for "@ViewBag.SearchTerm"</h1>

    <div class="container">
        <div class="row">
            @foreach (var product in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card product-card">
                        <img src="@Url.Content("~/images/" + product.ImageUrl)" class="card-img-top product-image" alt="Product Image">
                        <div class="p-4 border border-secondary border-top-0 rounded-bottom">
                            <h4>@product.Name</h4>
                            <p>@product.Description</p>
                            <div class="d-flex justify-content-between flex-lg-wrap">
                                <span class="product-price text-muted">Price: </span>
                                <p class="text-primary mb-0">@product.Price.ToString("#,##0") Vnđ</p>
                                <a asp-controller="ShoppingCart" asp-action="AddToCart" asp-route-productId="@product.Id" asp-route-quantity="@numOfQuantity">
                                    <button class="btn btn-outline-dark">
                                        <i class="bi-cart-fill me-1"></i> Add to Cart
                                    </button>
                                    <button class="btn btn-outline-dark">
                                        <i class="bi-cart-fill me-1"></i> Back
                                    </button>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>

    <!-- Script Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
